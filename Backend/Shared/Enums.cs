namespace SharedLib;

public enum RoomTableStatusType
{
    Unreserved = 0,
    Reserved = 5,
    Busy = 10
}

public enum ReservationStatusType
{
    Active = 0,
    Cancelled = 5,
    Completed = 10
}
